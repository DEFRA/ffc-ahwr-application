// const createReference = require('../../../../app/lib/requires-compliance-check')
describe('Test requires compliance check', () => {
  test('Compliance check required', async () => {
    const a = 1
    expect(a).toBe(1)
  })
})
